<%= render  "navbar" %>
<h1 class="text-center my-5 font-medium text-4xl text-indigo-900">Utilisateurs</h1>
<div class="flex flex-col mt-6">
  <div class="-my-2 overflow-x-auto sm:-mx-6 lg:-mx-8">
    <div class="py-2 align-middle inline-block min-w-full sm:px-6 lg:px-8">
      <div class=" overflow-hidden sm:rounded-lg">
        <table class="w-3/4 mx-auto divide-y divide-gray-200">
          <thead class="bg-gray-200">
            <tr>
              <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
                email
              </th>
              <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
                Admin
              </th>
              <th scope="col" class="relative px-6 py-3">
                <span class="sr-only">Edit</span>
              </th>
            </tr>
          </thead>
          <tbody class="bg-white divide-y  divide-gray-200">
          <% @users.each do |user| %>
            <tr class="border-b border-gray-300">
              <td class="px-6 py-4 whitespace-nowrap text-sm font-medium text-gray-900">
                <%= user.email %>
              </td>
              <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-500">
                <%= user.is_admin %>
              </td>
              <td class="px-6 py-4 whitespace-nowrap text-right text-sm font-medium">
                <% if user.is_admin  %>
                  <%= link_to "Passer non admin", set_user_admin_path(user.id), class: "text-green-600 hover:text-green-900"  %>
                <% else %>
                  <%= link_to "Passer admin", set_user_admin_path(user.id), class: "text-green-600 hover:text-green-900"  %>
                <% end %>
                <%= link_to "Supprimer", "/admin/users/#{user.id}", method: :delete, class: "text-red-600 ml-3 hover:text-red-900"  %>
              </td>
            </tr>
          <% end %>
            <!-- More people... -->
          </tbody>
        </table>
      </div>
    </div>
  </div>
</div>